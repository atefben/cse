{% extends 'base.html.twig' %}

{% block body %}
    <h1>Surveycriterium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ surveyCriterium.id }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ surveyCriterium.score }}</td>
            </tr>
            <tr>
                <th>Coefficient</th>
                <td>{{ surveyCriterium.coefficient }}</td>
            </tr>
            <tr>
                <th>Additionalnote</th>
                <td>{{ surveyCriterium.additionalNote }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('surveycriteria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('surveycriteria_edit', { 'id': surveyCriterium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
