{% extends 'base.html.twig' %}

{% block content_title %}
    Nouvelle liste de diffusion
{% endblock %}

{% block content %}
    <h1>Mailgroup creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('mailgroup_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
